# ── Core framework ────────────────────────────────────────────────────────────
gradio>=4.44.0
python-dotenv>=1.0.0
requests>=2.32.0

# ── Script generation (Ollama client) ────────────────────────────────────────
# Ollama itself is a binary — install from https://ollama.ai
# No extra Python package needed (uses REST API via requests)

# ── Narration: Piper TTS ──────────────────────────────────────────────────────
# Piper is a binary — install from https://github.com/rhasspy/piper/releases
# No extra Python package needed

# ── Music: MusicGen (audiocraft) ─────────────────────────────────────────────
audiocraft>=1.3.0
scipy>=1.13.0
numpy>=1.26.0

# ── Image generation: diffusers (SDXL / FLUX) ─────────────────────────────────
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.4
torch>=2.4.0                # CPU or CUDA — install the right variant for your system
torchvision>=0.19.0
Pillow>=10.4.0

# ── Subtitles: Whisper ────────────────────────────────────────────────────────
openai-whisper>=20240930

# ── YouTube upload ────────────────────────────────────────────────────────────
google-api-python-client>=2.143.0
google-auth>=2.34.0
google-auth-oauthlib>=1.2.1
google-auth-httplib2>=0.2.0

# ── Instagram upload ──────────────────────────────────────────────────────────
# Uses Meta Graph API via requests (already listed above)

# ── Video (FFmpeg Python wrapper for duration probe) ─────────────────────────
# ffmpeg binary is required — see Dockerfile or install guide
imageio[ffmpeg]>=2.34.0

# ── Optional: xformers for faster SDXL on CUDA ───────────────────────────────
# xformers>=0.0.27  # uncomment if using NVIDIA GPU
